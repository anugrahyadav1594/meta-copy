-r requirements.txt
pytest>=8.1
pytest-asyncio>=0.23
ruff>=0.4
black>=24.4
mypy>=1.10
# Embedded PostgreSQL 16 binaries used to run real integration tests
# without Docker (CI / local sandboxes). Not needed when using docker compose.
pgserver>=0.1.4; platform_system == "Linux"
